name: dinovdeau2_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - pandas
      - kornia
      - datasets
      -huggingface-hub==0.23.1
      - transformers==4.41.0
      - scikit-learn
      - codecarbon
      - tensorboardX
      - accelerate
      - tabulate
      - ipykernel
      - pillow
      - importlib-resources
