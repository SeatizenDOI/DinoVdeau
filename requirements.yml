name: dinovdeau_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - pandas
      - kornia
      - datasets
      - transformers
      - scikit-learn
      - codecarbon
      - tensorboardX
      - accelerate
      - tabulate
      - ipykernel
      - pillow
      - importlib-resources
